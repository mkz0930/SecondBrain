# 需求文档

## 1. 项目概述
**外挂大脑（Second Brain）** 是一个个人知识管理工具，旨在帮助用户积累和组织日常见闻、随笔、文章、音视频和书籍等多类型内容。系统支持本地部署，并通过飞书多维表格实现移动端数据的双向同步。

## 2. 核心功能需求

### 2.1 内容管理
- **多类型支持**：支持随笔、文章、音视频、书籍四种基本内容类型。
- **CRUD操作**：支持内容的创建、读取、更新和删除。
- **属性管理**：
  - 标题、正文、摘要、URL链接、来源。
  - 评分系统（1-5星）。
  - 收藏状态标记。
  - 自动记录创建时间和更新时间。

### 2.5 AI内容分析
- **智能提取**：引入免费大模型（如Gemini），自动分析输入内容。
- **字段提取**：
  - 自动提取标题至标题字段（字数限制50字以内）。
  - 若内容包含URL，自动提取至URL字段。
- **URL解析**：自动读取提取到的URL内容。
- **智能摘要**：根据内容（或URL内容）生成摘要。
- **智能分类**：自动更新分类结果为：随便、抖音、公众号、文档、或其他。
- **每日总结**：
  - 每天晚上8点自动执行。
  - 汇总当日所有笔记，生成精炼日报（100-300字）。
  - 展示在时间线视图的日期下方，一横排展示。
  - 自动回溯生成历史数据的日报总结。

### 2.2 标签系统
- **自定义标签**：支持创建和管理自定义标签。
- **标签关联**：一个内容可关联多个标签。
- **颜色标记**：支持为标签设置不同颜色。
- **标签统计**：统计每个标签下的内容数量。

### 2.3 搜索与筛选
- **全文搜索**：支持对标题、正文、来源进行关键词搜索。
- **多维筛选**：
  - 按内容类型筛选。
  - 按收藏状态筛选。
  - 按标签筛选。

### 2.4 数据统计
- **概览统计**：展示内容总数、标签总数、收藏总数。
- **分布统计**：展示各类型内容的分布情况。
- **访问统计**：记录并展示最近访问和最常访问的内容。

## 3. 飞书集成需求

### 3.1 双向同步
- **本地到飞书 (Push)**：检测本地新增、修改、删除的内容，自动推送到飞书多维表格。
- **飞书到本地 (Pull)**：获取飞书表格的新增和修改记录，同步回本地数据库。
- **全字段覆盖**：同步标题、类型、正文、来源、评分、收藏状态、标签、时间等所有核心字段。

### 3.2 冲突处理
- **冲突检测**：识别同一记录在两端同时被修改的情况。
- **解决策略**：采用**飞书端优先**策略，当冲突发生时，飞书的数据覆盖本地数据。

### 3.3 自动同步
- **定时任务**：支持配置定时自动同步（默认15分钟）。
- **开关控制**：支持启用/禁用自动同步功能。

### 3.4 可靠性与安全
- **错误重试**：API调用失败自动重试。
- **限流处理**：智能处理飞书API限流。
- **字段自适应**：自动适配飞书表格字段，防止字段不匹配导致同步失败。
- **敏感信息加密**：对App Secret等敏感配置进行加密存储。
- **同步日志**：完整记录每次同步的详细信息（成功、失败、冲突数等），便于排查。

## 4. 非功能性需求
- **UI风格**：未来科技风，简约大气，偏暗色（参考 Google AI Studio）。
- **数据隐私**：核心数据存储在本地 SQLite 数据库。
- **部署方式**：支持 Windows/Linux/Mac 多平台部署。
- **启动便捷**：提供一键启动脚本。

## 5. 版本历史

### 2026-01-17 更新
| 类型 | 内容 | 负责人 |
| :--- | :--- | :--- |
| **UI优化** | 首页切换为时间线视图，按日期分组展示内容，每日之间增加明显区分。 | |
| **交互优化** | 分页模式由传统的页码翻页改为“加载更多”模式，提升浏览体验。 | |
| **字段扩展** | 新增“摘要”字段，支持手动输入或AI自动生成，并同步至飞书。 | |
| **文档重构** | 整理文档结构，统一为需求文档和各功能模块文档。 | |
| **启动优化** | 优化启动脚本（`start.ps1` 和 `start.py`），智能检测 Node.js 版本，移除重复的版本切换确认，实现一键无感启动。 | |
| **Bug修复** | 修复 `start.py` 在旧版本 Python 环境下因 `text` 参数导致的启动报错问题，兼容更多 Python 版本。 | Trae |
| **Bug修复** | 修复未登录状态下首页显示“暂无内容”的问题，现在默认以管理员身份展示数据。 | Trae |
| **Bug修复** | 修复飞书同步失败时前端错误提示不明确的问题，并修复了数据库中同步配置归属错误导致的鉴权失败。 | Trae |
| **体验优化** | 优化详情页（副页）展示，新增封面图展示（自动提取Markdown/HTML图片或URL），增加字数统计、预计阅读时间和手动评分展示，优化侧边栏布局。 | Trae |
| **功能增强** | 详情页新增“附件”模块，自动提取并展示正文中的所有图片及文件链接，支持点击预览和下载。 | Trae |
| **同步优化** | 增强飞书同步的字段兼容性，自动过滤不存在的字段，防止同步中断；增加关键字段缺失检测与告警日志。 | Trae |
| **系统升级** | 引入 `winston` 日志系统，替代控制台输出，实现按天滚动的错误日志与全量日志文件记录，便于长期运维与故障排查。 | Trae |
| **交互优化** | 优化飞书同步按钮交互，新增加载动画、进度条及 Toast 结果提示，提升用户感知。 | Trae |
| **UI优化** | 修复首页侧边栏在长页面滚动时无法固定的问题，优化页面滚动交互。 | Trae |
| **UI优化** | 优化侧边栏布局，将“内容类型”与“标签”合并，只展示 Top 8 热门标签作为主要分类，剩余标签折叠收起，解决侧边栏过长的问题。 | Trae |
| **UI优化** | 再次调整侧边栏布局，改为双列并排结构：左列展示“分类”和“收藏”，右列展示“标签”，有效利用横向空间，减少垂直滚动。 | Trae |
| **UI优化** | 调整页面整体布局，取消居中限制，改为全宽布局。侧边栏固定在屏幕最左侧，宽度采用自适应比例（minmax(360px, 25%)），确保在大屏下不超过 35% 占比，充分利用屏幕空间。 | Trae |
| **同步修复** | 修复飞书同步时“内容”字段丢失的问题，新增智能字段名匹配逻辑，支持 `内容`/`正文`/`Content` 等多种别名映射。 | Trae |
| **同步调整** | 调整同步冲突解决策略：当发生冲突或更新时，**内容**字段以飞书为准（飞书->本地），其他元数据字段以本地为准（本地->飞书），实现混合字段合并。 | Trae |
| **UI优化** | 优化详情页布局，将“附件列表”和“来源信息”从左侧栏移动到右侧主要内容区域，使信息展示更加流畅。 | Trae |
| **功能增强** | 新增“每日总结”功能，每晚8点自动汇总当日笔记生成日报，并展示在首页时间线日期下方；自动初始化历史数据的日报总结。 | Trae |
| **UI优化** | 详情页“来源信息”标题更名为“正文内容”，与用户使用习惯保持一致。 | Trae |
| **同步修复** | 修复飞书同步中“正文”、“收藏”及“url”字段因大小写或别名匹配问题导致的同步缺失，增强了字段映射的鲁棒性。 | Trae |
| **逻辑优化** | 优化每日总结生成逻辑，改为基于本地时间（Localtime）统计，确保总结内容准确覆盖用户所在时区的“当天”全天记录。 | Trae |
| **同步优化** | 增强飞书同步的时间字段映射，新增对“时间”字段的支持，确保飞书记录的主时间属性正确对齐为内容的创建时间。 | Trae |
| **同步增强** | 增强飞书同步的错误处理机制：当遇到 `URLFieldConvFail` 时自动处理空值；当遇到 `MultiSelectFieldConvFail` 时自动降级重试（跳过标签/类型字段），确保核心内容顺利同步。 | Trae |
| **UI优化** | 详情页侧边栏新增“原文链接”卡片，展示并支持直接点击内容的URL；若无URL，则显示“暂无原文链接”占位符，保持布局一致性。 | Trae |
| **体验优化** | 详情页正文支持 Markdown 渲染，实现富文本展示（标题、列表、代码块等），优化阅读体验。 | Trae |

### 2026-01-18 更新
| 类型 | 内容 | 负责人 |
| :--- | :--- | :--- |
| **功能优化** | 限制AI生成的标题长度在50字以内，避免标题过长影响排版和阅读。 | Trae |


